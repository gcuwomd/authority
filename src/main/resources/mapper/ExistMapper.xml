<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.authority.mapper.ExistMapper">
    <select id="OrganizationExist" resultType="java.lang.Boolean">
        select count(*) from unity_organization where  organization_id=#{organizationId}
    </select>
    <select id="DepartmentExist" resultType="java.lang.Boolean">
        select count(*) from unity_department where  department_name=#{key} OR department_id=#{key}
    </select>
    <select id="WebsiteExist" resultType="java.lang.Boolean">
        select count(*) from unity_website where website_name   =#{key} OR website_id=#{key}
    </select>
    <select id="apiExist" resultType="java.lang.Boolean">
        select  count(*) from unity_system_api where api_url=#{apiUrl}
    </select>
    <select id="routeExist" resultType="java.lang.Boolean">
        SELECT count(*) from unity_route_list where route_url=#{routeUrl}
    </select>
    <select id="RoleExist" resultType="java.lang.Boolean">
        select count(*) from  unity_role where role_name=#{key} OR role_id=#{key}
    </select>

</mapper>